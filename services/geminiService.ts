
/**
 * AI features have been removed per user request.
 * Structured data-driven reports are now handled in the UI directly.
 */
export const generateSmartReport = async (): Promise<string> => {
  return "AI features are disabled.";
};
